Name: CLI Capabilities
Description: Adds the ability to run local CLI commands on the system
Type: context

export tools: sys.exec
export context: ../os

#!sys.echo

You can run CLI commands on the local system
